# MemoOrbit 제품 요구사항 문서 (PRD v2.0 - 통합 개정판)

## 1. 제품 정의 및 핵심 가치 (Product Purpose & Value)

### 1.1 제품 목적 (Why MemoOrbit?)
MemoOrbit는 스쳐 지나가는 아이디어를 가장 빠르고 편안하게 기록하고, AI(Gemini)와 궤도(Orbit) 시각화를 통해 파편화된 생각을 연결하며, 방치된 과거의 질문과 아이디어를 다시 발견하게 돕는 반응형 메모 웹 애플리케이션이다.

### 1.1.1 공식 브랜딩 및 메타데이터
- 공식 영문명은 `MemoOrbit`, 한글명은 `메모오빗`으로 표준화한다.
- 브라우저·검색·공유 화면의 전역 타이틀은 `MemoOrbit - 내 생각과 기록의 우주 궤도`를 사용한다.
- 전역 설명은 `시간과 시기를 따라 생각과 메모들이 궤도를 그리며 연결되는 AI 지식 아카이브 웹 앱`을 사용하며, 화면 로고·기본 커버·내보내기 파일명의 `MemoOrbit` 표기를 일관되게 유지한다.

### 1.2 사용자가 보고 싶어 하는 가치 (User Goal)
- **즉시 기록**: 생각의 흐름이 끊기지 않는 빠르고 직관적인 단일 캔버스 메모.
- **생각의 궤도화**: 내 메모들이 키워드와 문맥에 따라 하나의 우주 궤도처럼 연결되어 확장되는 구조.
- **아이디어 재발견**: 작성 후 잊혀진 질문이나 미완성 항목을 AI가 자동으로 찾아내어 실행으로 이끌어주는 경험.

### 1.3 핵심 특장점 (Key Features)
1. **단일 캔버스 리치 에디터**: iOS 메모 앱 수준의 깔끔한 UX와 선택 영역 리치 포맷팅, 자동 저장 지원.
2. **동적 문맥 분석 엔진 (`/api/gemini`)**: 정적 규칙 없이 본문 입력 중단 300ms 후 핵심 태그 3~5개와 감정 코멘트를 자동 추천.
3. **태그 궤도 탐색 (Tag Orbit)**: 태그 간의 관계를 행성 노드와 궤도선으로 시각화하여 관련 생각을 한눈에 구조화.
4. **시간 궤도 분석 (Timeline Stream)**: 기간별 메모를 분석하여 미완성 아이디어와 다시 볼 핵심 메모를 스마트 큐레이션.

---

## 2. 디자인 시스템 및 반응형 기준

### 2.1 시각 언어: Deep Space Glassmorphism
- **Main Background**: `#0F1117` (Deep Space Dark)
- **Card & Panel**: `rgba(26, 29, 38, 0.8)` + `backdrop-blur-md` (Nebula Slate)
- **Primary Accent**: `#E5A93C` (Orbit Gold) - 핵심 행동, 선택 상태, 시그니처 궤적
- **Secondary Accent**: `#FFC86B` (Starlight Amber) - 호버 및 보조 강조
- **Border & Line**: `#2A2E3D` (Orbit Line) - 카드 외곽선, 입력 테두리, 구분선
- **Text Main / Muted**: `#F3F4F6` (제목 및 본문) / `#9CA3AF` (보조 설명 및 메타데이터)

### 2.2 전역 레이어 계층 (z-index)
- `z-10`: 기본 캔버스, 날짜 선택 카드, 일반 컨텐츠
- `z-20`: PC LNB, 모바일 FAB, 스티키 타이틀
- `z-30`: 최상단 공통 네비게이션 바 (`sticky top-0`)
- `z-40`: 사이드 드로어, 드롭다운, 툴팁
- `z-50`: DatePicker 포털, 편집 모달, 바텀시트
- `z-60`: 전역 토스트, 커스텀 Confirm 모달

---

## 3. UI/UX 및 레이아웃 규격

### 3.1 모바일/태블릿 UI (1280px 미만)
- **하단 고정 탭 바 완전 제거**: 불필요한 하단 고정 영역을 삭제하여 뷰포트를 확보하고, 상단 햄버거 버튼(사이드 드로어)으로 이동을 단권화한다.
- **상단 헤더 슬림화**: 불명확한 우측 고정 버튼을 삭제하고, 좌측 햄버거 메뉴와 중앙 `MemoOrbit` 로고로 간결하게 구성한다.
- **상단 헤더 Fixed 고정**: 모바일 공통 헤더는 상위 컨테이너의 `overflow`와 관계없이 유지되는 `fixed inset-x-0 top-0 z-50`과 56px 높이를 사용한다. 반투명 Slate 배경·블러·옅은 하단 테두리를 적용하고, 문서 흐름에서 빠진 헤더 높이만큼 본문에 56px 상단 패딩을 확보한다.
- **새 메모 진입점**: 우측 하단 고정 FAB(`+` 버튼, `Orbit Gold` 배경)만 지원한다.
- **가로 오버플로우 차단**: `html`, `body`, 루트 레이아웃과 `main`은 `max-width: 100%` 및 `overflow-x: hidden`을 유지하며 카드에 `100vw`를 사용하지 않는다.

### 3.2 메모 목록 & 갤러리 카드 뷰
- **모바일 목록 노출 극대화**: `sm` 미만에서는 `ALL MEMOS` 라벨과 설명 문구를 숨기고, `모든 메모` 제목·전체 개수 배지·텍스트/갤러리 보기 전환을 하나의 행에 배치하여 상단 세로 공간을 최소화한다.
- **모바일 리스트 컴팩트 간격**: 텍스트 리스트 메모는 모바일에서 `py-2 px-3` 수준의 안쪽 여백을 사용하고 목록 및 날짜 그룹 사이의 간격을 줄여 한 화면에서 8~10개 항목을 빠르게 훑을 수 있게 한다.
- **모바일 날짜 그룹 구분 여백**: `오늘`·`어제`·`지난 7일`·`이전 30일`·`이전 기록` 제목이 앞선 고정 또는 날짜 목록 뒤에 이어질 때 제목 위에 12px 여백을 둔다. 화면의 첫 날짜 그룹에는 추가 상단 여백을 만들지 않는다.
- **모바일 하단 플로팅 검색/작성 바**: 검색 아이콘·검색 입력·AI 검색 토글·새 메모 작성 버튼을 하나의 둥근 바에 합쳐 화면 하단에 고정한다. 작성 버튼은 Orbit Gold 원형 버튼으로 강조하며, 목록 끝은 `pb-24` 이상의 여백을 확보해 고정 바에 가리지 않게 한다. 데스크톱 검색·고급 필터 패널은 기존 문서 흐름을 유지한다.
- **모바일 통합 검색·필터 바 개정**: 모바일 하단 바는 검색 입력·활성 조건 개수 배지가 있는 필터 버튼·새 메모 작성 버튼만 제공하며, 별도의 AI 검색 토글과 도움말 버튼은 제거한다. 필터 버튼은 하단 바 위 `bottom-20` 바텀시트를 열고, 바텀시트에는 의미 기반 검색 안내와 태그·미디어·상태·기간 조건을 표시한다.
- **모바일 가독성 균형 타이포그래피**: 텍스트 리스트는 모바일에서 제목 15px, 날짜·본문 미리보기 14px, 태그를 최소 `text-xs`, 컨테이너 `py-2 px-3`으로 사용한다. 지나친 축소를 피하면서도 각 메모의 위아래에 8px 안쪽 여백을 확보해 답답하지 않은 밀도를 유지한다.
- **모바일 Sticky 제목 바**: 메모 화면의 제목·전체 개수·보기 전환 행은 공통 56px 헤더 바로 아래인 `sticky top-14 z-30`에 고정하고 반투명 배경·블러·하단 테두리를 적용한다.
- **모바일 Sticky 제목 바 수직 균형**: `모든 메모`·전체 개수·보기 전환이 놓인 제목 행은 위아래에 동일한 8px 안쪽 여백을 적용하여 로고 헤더와 목록 사이에서 시각적으로 한쪽에 치우치지 않게 한다.
- **모바일 Sticky 제목 바 크기 균형**: `모든 메모` 제목은 바로 아래 섹션 제목인 `고정됨`과 같은 18px로 표시한다. 전체 개수 배지는 11px, 텍스트·갤러리 보기 아이콘은 14px로 키워 제목 행의 정보가 목록 섹션보다 지나치게 작아 보이지 않게 한다.
- **모바일 전체 화면 필터 모달**: 필터 패널은 `top-14 bottom-0 z-50` 전체 화면으로 열고, 뒤에는 `bg-black/80` 딤을 표시한다. 딤 또는 닫기 버튼으로 닫을 수 있으며 열린 동안 문서 스크롤을 잠근다. 상단에는 활성 조건 개수와 개별 해제가 가능한 선택 필터 칩을 표시한다.
- **작성 진입점 단일화**: 모바일의 독립 원형 FAB는 제거하고 하단 플로팅 바 내부의 `✏️ 작성` CTA만 새 메모 진입점으로 사용한다.
- **메모 목록 메타데이터 표현**: 목록 날짜는 `YYYY.MM.DD.`처럼 일 뒤에도 마침표를 붙이고 본문 미리보기와 공백으로 구분한다. 날짜와 태그에는 강조색을 사용하지 않고 중립 보조 텍스트 색상을 적용한다.
- **모바일 상단 연속 배치**: 공통 로고 헤더의 높이는 `main` 바깥 여백으로 확보하여 Sticky 타이틀이 로고 구분선 바로 아래에서 시작하게 한다. 타이틀 이후에는 인위적인 빈 높이를 두지 않고 고정 메모와 일반 목록을 온전히 이어서 표시한다.
- **직접 입력 기간 요약 및 초기화**: 선택 필터의 사용자 지정 기간은 `직접 입력`이라는 프리셋 이름 대신 실제 시작일과 종료일을 표시한다. 상세 필터 상단에는 검색어를 제외한 태그·기간·미디어·상태 조건을 한 번에 해제하는 `초기화` 버튼을 제공한다.
- **iOS 메모식 2단계 고정 스와이프**: 짧게 오른쪽으로 밀고 놓으면 1단계로 고정·해제 글자가 들어가는 74px 버튼 폭만 정확히 열린다. 여기서 카드를 되돌리거나 버튼을 직접 누를 수 있다. 2단계는 고정된 픽셀 임계값을 사용하지 않고 카드 실제 너비의 80% 이상, 즉 오른쪽 끝까지 충분히 길게 민 상태에서 손을 뗄 때만 고정·해제를 실행한다. 풀 스와이프 전에는 고정 상태를 바꾸지 않으며 짧은 이동은 원위치로 스냅백한다.
- **스와이프 방향별 액션 분리**: 카드를 오른쪽으로 미는 동안에는 왼쪽의 고정·해제 액션만 노출하고 우측의 더 보기·삭제 버튼은 렌더링 영역과 포인터 입력에서 완전히 숨긴다. 반대로 왼쪽으로 미는 동안에는 우측 액션만 노출한다.
- **열린 스와이프 자동 닫기**: 한 번에 하나의 카드만 액션 영역을 열 수 있고, 열린 카드 바깥을 마우스나 터치로 누르면 즉시 닫힌다.
- **2열 컴팩트 그리드 적용**: 거대했던 1행 1열 구조를 폐지하고 **2열 가변 그리드(`grid-cols-2 gap-3`)**를 적용한다.
- **컴팩트 썸네일**: 이미지 비율을 `4:3` 또는 `aspect-video` 크기로 축소하여 한 화면에서 다수의 메모를 스캔할 수 있도록 한다.
- **보기 전환 토글**: 텍스트·갤러리 전환은 전체 폭 버튼이 아닌 우측 정렬된 작은 세그먼트 토글로 제공한다.
- **사진형 목업 데이터**: 초기 메모 200개 모두에 태그·본문 주제에 맞는 프로젝트 로컬 사진 자산을 연결해 실제 사진 업로드 갤러리처럼 검수할 수 있게 한다.
- **기본 커버 노출 범위**: MemoOrbit 기본 커버는 이미지가 없는 메모를 사진형 갤러리 카드로 조회할 때만 표시하고, 신규 작성·기존 편집 모달과 회상 카드에서는 표시하지 않는다.

### 3.3 태그 궤도 탐색 (Tag Orbit)
- 명칭을 기존의 '생각 궤적 탐색'에서 **'태그 궤도 탐색 (Tag Orbit)'**으로 개정한다.
- 모바일 환경에서 태그 행성(노드) 클릭 시 팝업을 띄우지 않고, 그래프 하단 인라인 영역에 관련 메모 목록이 즉시 세로로 확장 노출되도록 한다.
- 모바일 태그 행성은 연결된 메모 수에 따라 **대형(8개 이상)·중형(4~7개)·소형(3개 이하)**으로 크기와 글로우 강도를 구분한다.
- 모바일 궤도 패널은 화면 상단에 고정하고, 선택한 태그의 관련 메모 목록만 남은 높이 안에서 독립적으로 세로 스크롤한다.
- 모바일 관련 메모 헤더에는 별도의 `닫기` 버튼을 표시하지 않고, 다른 태그를 선택하는 흐름으로 목록을 전환한다.
- 동적 행성 크기 검수용 초기 목업은 200개를 제공하며 `개발 70`, `기록 44`, `일상 30`, `육아 20`, `여행 12`, `요리 6`, `독서 4`의 의도적인 빈도 편차를 유지한다.
- 데스크톱 태그 궤도의 휠 확대·축소는 `{ passive: false }` 네이티브 이벤트 리스너로 처리하여 `preventDefault()` 호출 오류와 페이지 스크롤 간섭을 방지하고, 배율은 `0.55~2.4` 범위로 제한한다.
- 데스크톱 태그 궤도를 드래그할 때 중심 이동 좌표는 가로·세로 각각 최대 `±250px`로 제한하여 노드가 화면 밖으로 이탈하거나 구석에 쏠리지 않게 한다.
- **AI Orbit Node Linking**: Gemini는 메모의 제목·본문·태그 문맥을 비교해 `0~1` 가중치와 짧은 연관 이유를 가진 메모 간 링크를 생성한다. 기존 태그 행성 좌표·줌·드래그 연산은 유지하고, 연결된 메모의 대표 태그 행성 사이에는 가중치에 비례한 황금색 Glow 연결선을 추가 렌더링한다.
- 연관 분석은 링크가 없는 메모가 있을 때 자동 실행하고, 존재하지 않는 메모 ID·자기 참조·중복 링크·범위를 벗어난 가중치는 클라이언트와 서버에서 제거한다. 분석 결과는 `Memo.links`에 양방향으로 병합되어 IndexedDB에 함께 저장된다.

### 3.4 시간 궤도 분석 (Timeline Stream)
- `groupMemosByTime` 순수 함수는 메모의 `createdAt`과 현재 로컬 날짜의 자정 경계를 비교해 **오늘(`today`)·어제(`yesterday`)·지난 7일(`last7Days`)·이전 30일(`last30Days`)·이전 기록(`older`)**의 다섯 배열을 항상 반환한다.
- 오늘·어제는 각각 독립된 하루로 분리하고, 지난 7일은 2~7일 전, 이전 30일은 8~30일 전, 이전 기록은 30일 초과 또는 유효하지 않은 날짜로 분류한다. 각 배열은 원본을 변경하지 않고 `createdAt` 최신순으로 정렬한다.
- 시간 그룹 데이터는 기간 분석과 로컬 요약 파이프라인에서 그룹별 분포를 계산하는 데 사용하며, 날짜별 카드 목록을 별도로 중복 렌더링하지 않는다.
- Gemini 기간 분석이 권한·네트워크·서버 오류로 실패해도 화면을 중단하지 않고 로컬 총평과 핵심 주제 요약 목록을 표시하며, 사용자에게 로컬 분석 결과임을 명확히 안내한다.
- 시간 궤도 화면은 AI 감정·생각 분석과 잊혀진 아이디어 큐레이션에 집중하며, 하단에 날짜 구간별 메모 카드를 중복 렌더링하지 않는다.
- 시간 궤도 기간 선택기는 `1주일·1개월·3개월·1년·전체·직접 입력` 프리셋을 제공한다. 기간 프리셋은 오늘을 종료일로 삼아 시작일을 로컬 날짜 기준으로 계산하며, 모바일에서는 여섯 칩을 `grid-cols-6` 한 줄에 배치해 가로 스크롤을 사용하지 않는다.
- 시작일과 종료일은 기존의 독립 입력 박스와 달력 팝업 UI를 사용하되, 화면에 `시작일`·`종료일` 텍스트 라벨을 별도 행으로 표시하지 않고 `[날짜 박스] ~ [날짜 박스]` 한 줄로 배치한다. 접근성 이름은 유지한다. 프리셋 기간에는 두 입력을 실제 `disabled` 상태와 `opacity-40`으로 잠그고, `직접 입력`을 선택했을 때만 활성화한다. PC에서는 프리셋 그룹 옆에 같은 한 줄 날짜 영역을 수평 배치한다.
- 모바일 메모 화면의 본문은 고정 헤더 높이와 정확히 같은 `pt-14` 오프셋만 사용하여 로고 헤더와 Sticky 타이틀 사이 간격을 0px로 유지한다. 검색 UI는 고정 배치로 문서 공간을 차지하지 않으며, 실제 `memos-container`에만 `pt-2` 안전 여백을 두어 첫 고정 메모가 헤더에 가리지 않게 한다.
- 시간 궤도와 메모 상세 필터의 사용자 지정 기간은 공통 `DateInputBox`를 사용한다. 두 날짜 박스는 `직접 입력` 칩 바로 다음에 `[YYYY-MM-DD ✕ 📅] ~ [YYYY-MM-DD ✕ 📅]` 순서로 인라인 배치하며, 값이 있을 때만 삭제 버튼을 표시한다. 달력 버튼은 별도 모달이나 페이지 스크롤 잠금 없이 네이티브 `showPicker()`를 호출한다.
- 사용자 지정 날짜 행은 `sm` 미만에서 프리셋 칩 그룹 아래의 새 줄로 내려가 `w-full`을 사용하며 가로 스크롤을 만들지 않는다. `sm` 이상에서는 칩 그룹 오른쪽에 인라인 배치한다. 날짜 박스의 디자인·삭제 버튼·달력 호출 및 직접 입력 활성 조건은 그대로 유지한다.
- PC LNB와 모바일 헤더의 `MemoOrbit` 로고를 누르면 메모 목록으로 이동하면서 검색어·태그·기간·직접 입력 날짜·미디어·고정 조건을 모두 초기화하고 문서 최상단으로 이동한다. 열려 있던 상세 필터도 함께 닫는다.
- `DateInputBox`는 `[년-월-일 표시 영역 | ✕·📅 아이콘 영역]`의 분리형 박스로 렌더링한다. 실제 `type=date` 입력은 시각적으로 숨기고 달력 버튼이 `showPicker()`를 호출하며, 값이 있을 때만 달력 아이콘 왼쪽에 삭제 버튼을 표시한다.
- 상세 필터 전체 화면 레이어는 내부 빈 공간이나 배경을 눌러도 닫히지 않으며 우측 상단 `닫기 ✕` 버튼만 닫기 State를 변경한다.
- 날짜 입력 본체는 `#161922`, 아이콘 분리 영역은 `#121318`, 테두리는 `#2a2e3d`, 달력 아이콘은 Orbit Gold `#e5a93c`를 사용한다.
- 메모 목록 래퍼는 고정 헤더와 Sticky 타이틀의 합산 높이인 `pt-28`을 내부 오프셋으로 확보하되 같은 높이의 위치 보정을 적용해 화면상 빈 공간은 만들지 않는다. 최종적으로 첫 고정 메모 앞에는 8px 안전 간격만 남긴다.
- **메모 목록 상단 영역과 검색 입력 계층 분리**: 모바일 메모 본문은 공통 56px 고정 헤더 높이만큼 `pt-14`를 확보하고, 제목 바 바로 아래에서 `고정됨` 섹션과 일반 날짜 목록을 순서대로 온전히 표시한다. 목록 래퍼에는 음수 상단 여백을 사용하지 않고 8px 안전 여백만 둔다. PC 검색·필터 패널 위에는 목록 래퍼가 겹치지 않아 검색 입력, 필터 열기 및 모든 상세 조건 버튼이 정상적으로 포인터 입력을 받아야 한다.
- **모바일 메모 제목 바 스크롤 고정**: `모든 메모` 제목·전체 개수·보기 전환 행은 페이지를 스크롤하는 동안에도 공통 로고 헤더 바로 아래 `top-14` 위치에 계속 고정한다. 상위 레이아웃의 가로 오버플로우 차단은 Sticky 기준이 되는 문서 스크롤을 분리하지 않는 `overflow-x-clip` 방식으로 처리한다.
- **커스텀 달력 반응형 위치 보장**: PC 달력은 선택한 날짜 입력 박스 바로 아래에 좌·우 정렬해 표시하고, 모바일 달력은 좌우 16px 안전 여백을 둔 화면 중앙 고정 레이어로 표시한다. 달력의 이전·다음 버튼, 요일 7열과 모든 날짜는 뷰포트 및 앱 루트의 경계에 잘리지 않아야 한다.
- **검색 필터 달력 레이어 보장**: PC 메모 검색 패널은 `backdrop-blur`로 생성되는 독립 stacking context 자체를 메모 목록보다 높은 레이어에 배치한다. 직접 입력 달력은 패널 아래로 펼쳐져도 고정 메모·날짜 그룹·메모 카드에 가려지지 않아야 한다.
- 시간 궤도와 메모 필터의 날짜 버튼은 네이티브 피커 대신 공통 다크 커스텀 달력 팝업을 연다. 팝업은 월요일부터 일요일까지의 7열 그리드, 이전·다음 달 이동, Orbit Gold 선택일을 제공하며 입력 바깥 클릭과 Escape로 닫힌다.
- 모바일 사이드바의 딤 백드롭은 `z-[90]`, 드로어는 `z-[100]`을 사용하여 공통 로고 헤더와 하단 검색·작성 바보다 위에 표시한다. 백드롭을 누르면 드로어를 닫는다.

---

## 4. 에디터 & AI 연동 요구사항

- **단일 캔버스**: 제목과 본문을 하나의 영역에서 작성하며 첫 줄은 제목으로 자동 지정한다.
- **에디터 여백 포커스**: 모바일에서 본문 캔버스의 빈 여백이나 패딩을 누르거나 터치해도 `contentEditable` 에디터가 포커스를 받고 커서는 본문 맨 끝으로 이동한다.
- **블록 요소 키보드 삭제**: 이미지 또는 표가 선택되었거나 커서 바로 앞에 있을 때 `Backspace` 또는 `Delete` 입력으로 해당 블록 전체를 안전하게 제거하고 본문 상태를 즉시 동기화한다.
- **모바일 하단 도구 고정**: 에디터는 `100dvh` 세로 Flex 구조를 사용하고, 본문만 스크롤하며 AI 추천 태그와 빠른 서식 도구는 `flex-none` 하단 패널에 고정한다.
- **가상 키보드 밀착 툴바**: 모바일 서식 툴바는 Visual Viewport의 `resize`·`scroll` 변화를 구독해 가상 키보드 상단에 밀착하며, `가가` 버튼으로 여는 서식 팝업도 키보드 뒤가 아닌 키보드 바로 위에 표시한다. 키보드가 닫히면 모두 원래 하단 위치로 복귀한다.
- **태그 입력과 추천 목록**: 직접 추가 입력창과 컨테이너는 항상 `w-full`을 유지하고 너비 전환 애니메이션을 사용하지 않는다. 모바일 입력 글자 크기는 iOS 자동 확대가 발생하지 않는 `16px` 이상으로 유지한다. 추천 태그는 한 줄을 유지하는 가로 스크롤 목록으로 표시하며 스크롤바는 숨긴다.
- **서식 버튼 표기**: 모바일 서식 진입 버튼은 영문 `Aa`처럼 두 번째 `가`를 더 작게 표시하여 제목·본문 크기 조절 기능임을 직관적으로 전달한다.
- **이탈 시 자동 저장**: 작성 중 이탈 시 내용이 존재하면 무조건 자동 저장하고 `updatedAt`을 갱신한다.
- **MemoOrbit 기본 커버**: AI 이미지 무드 타입·랜덤 지정·자동 이미지 생성 로직을 사용하지 않으며, 사용자가 첨부한 `imageUrl`이 없는 메모를 사진형 갤러리 카드로 조회할 때만 다크 배경과 황금색 궤도 링으로 구성한 브랜드 커버를 표시한다.
- **모달 배경 스크롤 차단**: PC와 모바일에서 메모 작성·편집 모달 또는 팝업이 열리면 실제 문서 스크롤을 담당하는 `html`과 `body`를 함께 잠그고, 중첩 팝업까지 모두 닫힌 뒤 이전 스타일을 복원한다.
- **PC 스크롤바 시인성**: PC 전역 스크롤바는 짙은 다크 트랙 위에 기본 상태부터 밝은 Orbit Gold thumb를 사용하여 현재 위치가 즉시 보이게 한다.
- **새로고침 초기 위치**: 브라우저의 자동 스크롤 위치 복원을 비활성화하고, 앱을 새로 불러올 때 메모 목록 최상단에서 시작한다.
- **다중 이미지 대표 선별**: 여러 첨부 이미지는 URL과 원본 파일명을 함께 보존하고, 본문 핵심어·태그와 파일명의 일치 점수로 대표 `imageUrl`을 자동 선택하며 모두 미일치하면 첫 이미지를 사용한다.
- **서버 API 호출 (`app/api/gemini`)**: API 키를 노출하지 않으며, Gemini 모델 응답 실패 시 로컬 동적 핵심어 분석기로 자동 대체(Fallback)한다.
- **태그 추천 API (`app/api/tags/recommend`)**: `gemini-1.5-flash`가 메모 본문에서 조사·어미·`오늘`·`완료` 같은 불필요한 단어를 제외한 핵심 명사 태그를 최대 5개 추출하며, 응답은 `{ "tags": string[] }` JSON 객체로만 반환한다.
- **서버 오류 진단**: API 키 누락, 잘못된 요청, 모델 호출 실패 원인은 메모 본문과 API 키를 제외한 안전한 서버 로그로 기록하며, `error.cause`의 `code`, `errno`, `syscall`, `hostname`, `address`, `port`를 선별 직렬화한다.
- **시간 궤도 Gemini 진단**: `gemini-2.5-flash` 호출 실패 시 서버는 안전한 오류 세부 정보를 반환하고, 시간 궤도 클라이언트는 해당 원인을 개발자 콘솔에 기록한 뒤 로컬 분석으로 대체한다.

### 4.2 고급 검색 필터
- `filterMemos` 순수 유틸리티는 검색어, 다중 태그, 이미지·표 포함 여부, 고정 상태, 시간 범위를 하나의 교차 필터로 결합하며 원본 메모 배열을 변경하지 않는다.
- 다중 태그는 기본 `AND` 모드에서 선택 태그를 모두 가진 메모만, `OR` 모드에서 하나 이상 가진 메모를 반환한다. 태그와 검색어 비교는 앞뒤 공백과 영문 대소문자 차이를 무시한다.
- 시간 프리셋은 전체·이번 주(월요일 시작)·이번 달·최근 3개월·사용자 지정 범위를 지원한다. 사용자 지정 종료일은 해당 로컬 날짜의 마지막 시각까지 포함한다.
- 일반 검색 결과는 고정 메모 우선, 같은 상태에서는 `updatedAt` 최신순으로 정렬한다.
- Gemini 시맨틱 검색을 활성화하면 일반 텍스트 일치 결과와 양수의 `semanticScores`를 가진 메모를 합친 하이브리드 결과를 만들고, 다른 필터 조건을 동일하게 적용한 뒤 AI 연관도 점수 내림차순으로 정렬한다. 동점은 고정 상태와 최신 수정일 순으로 정렬한다.
- 메인 화면에는 텍스트 검색창과 AI 시맨틱 검색 토글을 항상 노출하고, 확장 패널에서 다중 태그·AND/OR·사진·표·고정·시간 프리셋을 칩 형태로 조작할 수 있는 `SearchFilterBar`를 제공한다.
- `SearchFilterBar`는 모든 선택값을 하나의 `MemoFilterOptions` 객체로 부모에 전달하며, 메인 페이지는 이를 `filterMemos`에 즉시 적용한다.
- 필터 결과는 메모 카드 목록과 `OrbitGraphView` 양쪽에 동일하게 전달하여 목록 카드와 태그 궤도 노드가 같은 검색 범위를 실시간으로 반영한다.
- 태그 조합 방식 선택 UI와 `tagMatchMode` 상태는 제공하지 않는다. 태그를 여러 개 선택하면 그중 하나라도 포함된 메모를 반환하는 OR 규칙을 항상 적용하며, 태그가 선택되지 않은 `전체` 상태에서는 태그 조건을 적용하지 않는다.
- 시간 프리셋에는 `직접 입력`을 추가하고, 선택 시 기존 날짜 선택 UI를 활용한 시작일·종료일 입력 영역을 필터 패널 하단에 노출한다.
- AI 검색 토글 옆에는 마우스와 터치로 열고 닫을 수 있는 안내 버튼을 배치하여 일반 문자열 검색과 문맥·의미 기반 검색의 차이를 쉬운 한글로 설명한다.
- 메모 목록의 모바일·태블릿 상단 제목 영역은 56px 고정 `MemoOrbit` 로고 바가 끝나는 즉시 시작하며, 공통 헤더의 추가 상단 패딩으로 빈 띠가 생기지 않게 한다.
- PC 메모 목록의 기존 제목·검색 영역 간격과 카드 밀도는 유지하고, 모바일 텍스트 목록만 태그 줄과 세로 간격을 압축하여 일반적인 세로 화면에서 약 10개 메모를 빠르게 훑어볼 수 있게 한다.
- 직접 입력 날짜 달력은 기존의 넓은 다크 패널 디자인을 유지한다. 일요일부터 토요일까지 7열로 표시하고, 원형 이전·다음 달 버튼과 둥근 사각형의 Orbit Gold 선택 날짜를 사용한다.

### 4.1 브라우저 데이터 영속화
- 메모는 IndexedDB의 `memos` Object Store에 `id`를 키로 한 개별 레코드로 비동기 저장하며, 전체 갱신은 하나의 readwrite 트랜잭션에서 원자적으로 처리한다.
- 앱 최초 로딩은 IndexedDB 데이터를 우선 사용하고, 저장된 레코드가 없을 때 기존 `memoorbit-memos` LocalStorage JSON을 한 번만 마이그레이션한다. IndexedDB 저장이 성공한 뒤에만 기존 LocalStorage 키를 제거한다.
- 두 저장소가 모두 비어 있거나 기존 JSON이 손상되었으면 최신 기본 메모를 IndexedDB에 초기화한다. IndexedDB를 사용할 수 없는 환경에서는 LocalStorage를 장애 안전망으로 사용해 메모 유실과 앱 중단을 방지한다.

---

## 5. 구현 검수 체크리스트

- [x] 단일 리치 텍스트 에디터 및 선택 영역 서식
- [x] 모바일 에디터 하단 태그·서식 도구 고정 및 AI 이미지 무드 기능 폐기
- [x] 사용자 첨부 이미지가 없는 사진형 갤러리 카드에만 MemoOrbit 기본 커버 그래픽 표시
- [x] 신규 작성·편집 모달 기본 커버 제거 및 PC 모달·팝업 배경 스크롤 락
- [x] PC 전역 골드·다크 스크롤바 및 모바일 태그 관련 메모 `닫기` 버튼 제거
- [x] `html`·`body` 통합 팝업 스크롤 락 및 중첩 잠금 안전 복원
- [x] PC 골드 스크롤바 대비 강화 및 새로고침 시 최상단 초기화
- [x] 다중 첨부 이미지 보존 및 본문 문맥 기반 대표 이미지 자동 선별
- [x] Deep Space Glassmorphism 디자인 시스템 준수
- [x] Gemini API 연동 동적 태그 추천 및 로컬 fallback
- [x] 모바일 하단 탭 바 제거 및 뷰포트 극대화
- [x] 갤러리 뷰 2열 컴팩트 그리드(`grid-cols-2`) 개편
- [x] 태그 궤도 탐색 타이틀 변경 및 모바일 인라인 리스트 노출
- [x] 태그 메모 개수 기반 동적 행성 크기 및 모바일 독립 스크롤 구조
- [x] 태그 궤도 크기 차등 검증용 편차 메모 데이터 200개
- [x] 태그 궤도 휠 Passive 이벤트 오류 수정 및 드래그 이동 범위 `±250px` 제한
- [x] Git Push 성공 직후 커밋 정보·초보자용 데이터 흐름·실무 핵심 원리를 포함한 3단계 최종 보고를 생략 없이 출력하도록 에이전트 지침 강화
- [x] 승인이 필요한 터미널 명령은 CLI 실행 도구를 직접 호출하여 `Allow`/`Deny` 승인 팝업을 노출하도록 에이전트 지침 강화
- [x] 시간 궤도 메모의 5개 날짜 구간 그룹화 및 Gemini 실패 시 로컬 총평·요약 목록 Fallback
- [x] 주제별 AI 생성 사진 자산 10종 및 사진 포함 목업 메모 200개
- [x] 모바일 가로 오버플로우 차단 및 우측 정렬 보기 전환 토글
- [x] DatePicker 독립 레이어(`z-50`) 및 외부 클릭 닫기
- [x] 에디터 여백 터치 시 본문 끝 포커스 및 이미지·표 블록 키보드 삭제
- [x] Gemini 1.5 Flash 태그 전용 API와 한 줄 가로 스크롤 추천 태그 UI
- [x] 태그 직접 입력 100% 너비 및 Visual Viewport 기반 모바일 툴바 위치 보정
- [x] 모바일 키보드 위 서식 팝업 배치·두 번째 `가` 축소·태그 입력 자동 확대 방지
- [x] 시간 궤도 하단 중복 카드 제거 및 모바일 공통 헤더 Sticky 글래스 스타일 적용
- [x] 모바일 공통 헤더 Fixed 완전 고정 및 본문 56px 상단 패딩 교정
- [x] `groupMemosByTime` 5구간 순수 함수 및 시간 궤도 분석 파이프라인 연결
- [x] LocalStorage 메모의 IndexedDB 비동기 마이그레이션 및 장애 Fallback
- [x] Gemini 메모 연관 분석·`Memo.links` 영속화·Orbit Glow 연결선 렌더링
- [x] MemoOrbit 공식 명칭 및 전역 메타데이터 표준화
- [x] PC 날짜 입력 320px 컴팩트 폭·2열 대칭·달력 팝오버 정렬
- [x] iOS 메모식 2단계 고정 스와이프 및 외부 포인터 자동 닫기
- [x] 다중 태그·미디어·상태·시간 프리셋·Gemini 점수를 결합한 고급 검색 필터 유틸리티
- [x] 검색 필터 UI 패널 및 메인 카드·태그 궤도 실시간 바인딩
- [x] 검색 필터 한글 용어·스마트 전체 선택·직접 날짜 입력·AI 안내 툴팁
- [x] 메모 로고 바와 목록 제목 사이의 추가 상단 여백 제거 및 기존 7열 달력 디자인 복원
- [x] PC 레이아웃 유지 및 모바일 10개 탐색 밀도·상단 로고 연결 간격 보정
- [ ] 회원 인증 시스템(NextAuth / Supabase) 연동
- [ ] 서버 기반 데이터 영속화(Database) 구축

---

## 6. 3단계 구현 로드맵

### 고도화 명세: IndexedDB 및 AI 노드 링킹
- 브라우저 메모 저장소는 `idb` 기반 `MemoOrbitDB` 버전 1의 `memos` Store를 사용하며, 최초 로드 시 기존 LocalStorage 데이터를 자동 마이그레이션한다.
- 저장소 API는 `initAndMigrateStorage`, `getAllMemos`, `saveMemo`, `deleteMemo` 비동기 함수로 표준화한다.
- 신규·수정 메모는 `/api/memos/link`에서 `gemini-1.5-flash`로 기존 메모와 비교하고, 유사도 `0.75` 이상인 `targetId`, `weight`, `reason`을 가진 순수 JSON 배열만 반환한다.
- 태그 궤도 캔버스는 검증된 `Memo.links`의 가중치에 따라 연결선 두께와 투명도를 바꾸고, 연결된 노드에 마우스를 올리면 Glow를 표시한다.
- React의 최신 메모 배열을 IndexedDB에 반영할 때 저장과 삭제를 하나의 `readwrite` 트랜잭션으로 묶어 중간 상태가 남지 않게 한다.

### 1단계: 기록과 탐색 기반 (완료)
- 단일 리치 텍스트 캔버스, 체크리스트, 표, 동적 태그, 최신 수정순 그룹 목록.

### 2단계: 시각화 및 추억 경험 (진행 중)
- AI 이미지 생성 및 캐싱, 2열 컴팩트 갤러리 뷰, 태그 궤도 탐색(Tag Orbit) 인라인화.

### 3단계: 회상, 회원 인증 및 데이터 영속화 (차기 진행)
- 사용자 인증(OAuth/이메일), Multi-tenant DB 연동, 공유용 카드 이미지 내보내기.
