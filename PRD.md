# MemoOrbit 제품 요구사항 문서

## 1. 제품 목표

MemoOrbit는 아이폰 메모 앱처럼 빠르고 편안하게 기록하면서, 서식·추천 태그·연관 검색으로 생각을 확장하는 반응형 메모 서비스다. 휴대폰과 태블릿에는 동일한 터치 중심 UI를 제공하고, 데스크톱에서만 넓은 화면용 UI를 제공한다.

## 2. 반응형·접근성 기준

- 모바일·태블릿: 데스크톱 미만(`1280px` 미만)에서 전체 화면 편집기, 구분선 메모 목록, 터치 중심 조작을 사용한다.
- 데스크톱: `1280px` 이상에서 중앙 카드 목록, 선택 영역 서식 버블, 명시적인 메모 작업 버튼을 사용한다.
- 모바일 편집기는 `100dvh`와 안전 영역을 사용하며 테두리와 둥근 외곽선을 제거한다.
- 본문과 배경, 버튼 라벨과 버튼 배경은 WCAG AA 일반 텍스트 기준 4.5:1 이상의 대비를 확보한다.
- 토글 상태는 색과 함께 `aria-pressed` 또는 `aria-expanded`로 전달한다.

## 3. 에디터 요구사항

### 3.1 단일 리치 텍스트 캔버스

- 제목과 본문을 하나의 `contentEditable` 캔버스에서 편집하고 첫 줄은 제목으로 저장한다.
- 굵게, 기울임, 밑줄, 취소선, 글자·강조색, 문단, 목록, 체크리스트, 표를 지원한다.
- 문자 서식은 `window.getSelection()`으로 확인한 캔버스 내부의 비어 있지 않은 선택 범위에만 적용한다.
- 서식 버튼을 누르는 동안 선택 범위를 보존한다.
- 캔버스에는 브라우저의 기본 터치·마우스 드래그 선택을 방해하는 스와이프나 제스처를 등록하지 않는다.

### 3.2 모바일 포맷 도구

- 하단 빠른 실행 영역에 `가가`, `체크리스트`, `표` 버튼을 수평 배치한다.
- `가가` 패널에는 본문을 가리는 딤이나 전체 화면 오버레이를 사용하지 않는다.
- 패널이 열린 상태에서도 본문과 실시간 서식 결과가 보인다.
- 데스크톱에서는 선택한 텍스트 가까이에 포맷 버블을 표시한다.

### 3.3 체크리스트·표·태그

- 체크리스트는 원형 체크 아이콘과 독립된 텍스트 편집 영역으로 구성한다.
- 원형 아이콘을 직접 누를 때만 완료 상태가 바뀌며 행의 나머지 영역은 커서 이동과 입력에 사용한다.
- 완료 항목은 취소선으로 표시한다.
- 표는 빈 2×2 셀, 폭 100%, 고정 레이아웃, 셀 안 자동 줄바꿈을 사용한다.
- 추천 태그는 누를 때 적용과 해제를 토글한다.
- 적용 태그는 진한 브랜드 배경과 흰 글자, 미적용 태그는 밝은 배경과 선명한 아웃라인으로 구분한다.

## 4. 메모 목록과 관리

- 메모는 수정 시각 최신순으로 정렬하고 고정 메모를 일반 메모 위에 배치한다.
- 고정 메모 섹션은 `aria-expanded`를 가진 아코디언으로 접고 펼친다.
- 카드 본문에 중복되는 `고정됨` 텍스트를 표시하지 않는다.
- 모든 디바이스에서 핀 버튼으로 고정 여부를 바꿀 수 있고 터치 환경에서는 스와이프도 지원한다.
- 모바일·태블릿 목록은 둥근 카드나 외곽선 없이 행 사이 구분선으로 나눈다.
- 데스크톱 목록은 카드 표면과 수정·공유·삭제 작업 버튼을 사용한다.
- 삭제는 브라우저 기본 확인창 대신 커스텀 모달을 사용한다.
- 제목만 있는 메모에는 `추가 텍스트 없음`을 표시한다.
- 데스크톱 `새 메모` 버튼은 브랜드 색을 유지하고 호버 시 자연스러운 어두운 색으로 바뀐다.

## 5. 구현 검수 체크리스트

- [x] 선택 범위에만 문자 서식 적용
- [x] 편집 캔버스의 드래그 선택 방해 제스처 제거
- [x] 모바일 `가가`·`체크리스트`·`표` 빠른 버튼 배치
- [x] 모바일 포맷 패널 딤·오버레이 제거
- [x] 체크박스 아이콘과 텍스트 영역 클릭 분리
- [x] 추천 태그 활성·비활성 색상 및 접근성 상태 구분
- [x] 모바일·태블릿 전체 화면 편집기와 구분선 목록 적용
- [x] 데스크톱 새 메모 호버 색상 정상화
- [x] 태블릿을 모바일 UI 범위에 통합
- [x] 4.5:1 이상 텍스트 대비 적용
- [x] 고정 메모 아코디언과 중복 문구 제거
- [x] 전 디바이스 핀 버튼 및 터치 스와이프 고정 제공
- [x] `npx tsc --noEmit` 통과
- [x] `npx eslint .` 통과

## 6. 이후 범위

- [ ] 서버 기반 AI 문맥 태그 추천
- [ ] 의미 기반 연관 메모 검색(Orbit Search)
- [ ] 사용자 인증 및 서버 영속 저장
