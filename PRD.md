# MemoOrbit 제품 요구사항 문서

## 1. 제품 목표

MemoOrbit는 아이폰 메모 앱처럼 빠르고 편안하게 기록하면서, 서식·추천 태그·연관 검색으로 생각을 확장하는 반응형 메모 서비스다. 휴대폰과 태블릿에는 동일한 터치 중심 UI를 제공하고, 데스크톱에서만 넓은 화면용 UI를 제공한다.

- 제품명과 상단 브랜드 표기는 항상 `MemoOrbit`를 사용하며 외부 저장 서비스명이나 운영체제 계정명으로 대체하지 않는다.

## 2. 반응형·접근성 기준

- 전체 시각 언어는 `Deep Space Minimalism & Glassmorphism`을 기준으로 하며 iOS 메모 앱의 간결한 정보 계층과 터치 경험을 결합한다.
- 편집기 상단에는 뒤로가기, 실행 취소, 공유, 더 보기, 노란색 원형 완료 버튼과 `연도년 월월 일일 시:분` 형식의 중앙 날짜를 배치한다.

- 모바일·태블릿: 데스크톱 미만(`1280px` 미만)에서 전체 화면 편집기, 구분선 메모 목록, 터치 중심 조작을 사용한다.
- 데스크톱: `1280px` 이상에서 `max-w-5xl` 중앙 컨테이너, 선택 영역 서식 도구, 명시적인 메모 작업 버튼을 사용한다.
- 모바일 편집기는 `100dvh`와 안전 영역을 사용하며 테두리와 둥근 외곽선을 제거한다.
- 본문과 배경, 버튼 라벨과 버튼 배경은 WCAG AA 일반 텍스트 기준 4.5:1 이상의 대비를 확보한다.
- 토글 상태는 색과 함께 `aria-pressed` 또는 `aria-expanded`로 전달한다.
- 키보드만으로 모든 메뉴, 입력, 팝오버, 날짜 선택 기능을 탐색하고 실행할 수 있어야 하며 WCAG 2.1 AA를 준수한다.

### 2.1 디자인 시스템 팔레트: Deep Space Glassmorphism

- 메인 배경(`Main Background`): `#0F1117`을 사용하며 깊은 우주 공간을 표현하는 `Deep Space Dark` 토큰으로 정의한다.
- 카드 및 패널(`Card & Panel`): `rgba(26, 29, 38, 0.8)` 배경과 `backdrop-blur-md`를 결합한 `Nebula Slate` 글래스모피즘 표면을 사용한다.
- 시그니처 포인트(`Primary Accent`): `#E5A93C`의 `Orbit Gold`를 핵심 행동, 현재 선택 상태, 주요 궤적에 사용한다.
- 보조 포인트(`Secondary Accent`): `#FFC86B`의 `Starlight Amber`를 호버, 보조 강조, 그래프 하이라이트에 사용한다.
- 테두리 및 구분선(`Border & Divider`): `#2A2E3D`의 `Orbit Line`을 카드 외곽선, 입력 테두리, 목록 구분선에 사용한다.
- 기본 텍스트(`Text Main`): `#F3F4F6`을 제목, 본문, 핵심 수치에 사용한다.
- 보조 텍스트(`Text Muted`): `#9CA3AF`를 설명, 날짜, 메타데이터에 사용한다.
- 글래스 패널은 투명도만으로 경계를 표현하지 않고 `Orbit Line` 테두리를 함께 사용하여 저시력 환경에서도 표면 경계를 구분할 수 있어야 한다.
- 포인트 색은 장식 목적과 상태 전달을 분리하고, 선택 상태에는 아이콘·텍스트·`aria-pressed` 같은 비색상 단서를 함께 제공한다.

#### 전역 레이어 계층

- `z-0`부터 `z-10`은 기본 캔버스, 날짜 선택 카드, 리포트 카드와 일반 콘텐츠에 사용한다.
- `z-20`은 PC LNB, 모바일 FAB와 페이지 메인 타이틀 스티키 영역에 사용한다.
- `z-30`은 불투명 Deep Space 배경의 `sticky top-0` 최상단 공통 네비게이션 바에 사용한다.
- `z-40`은 모바일 하단 탭 바, 사이드 드로어, 드로어 딤드, 드롭다운과 툴팁에 사용한다.
- `z-50`은 body 포털 DatePicker 달력, 편집 모달과 연관 메모 바텀시트에 사용한다.
- `z-60`은 전역 토스트와 커스텀 Confirm처럼 다른 모든 조작을 차단하는 최상위 피드백에 사용한다.
- 위 단계 밖의 임의 `z-index`를 새로 추가하지 않는다.

### 2.2 전역 메뉴 내비게이션

- 모바일·태블릿은 상단 햄버거 버튼으로 여는 사이드 드로어를 사용한다.
- 데스크톱은 화면 왼쪽에 고정된 `LNB`를 사용하고 콘텐츠 영역은 LNB 폭만큼 분리한다.
- 전역 메뉴는 `메모 목록(All Memos)`, `생각 궤적 탐색(Orbit Graph)`, `시간 궤도 분석(Timeline Stream)`의 세 축으로 구성한다.
- Orbit Graph는 태그 빈도와 동시 출현 관계를 행성 노드와 연결 궤도로 탐색하고 선택 태그의 메모를 바로 연다.
- Timeline Stream은 커스텀 시작일·종료일 피커와 미완성 궤도·다시 볼 주요 메모 리포트를 제공한다.

#### 플랫폼별 입력 UX 분리

- `1280px` 이상 PC에서는 마우스·휠·키보드 입력에 최적화된 고정 LNB와 와이드 분석 레이아웃을 사용한다.
- `1280px` 미만 모바일·태블릿에서는 터치 입력에 최적화된 사이드 드로어, 스와이프 카러셀, 바텀시트를 사용한다.
- PC Orbit Graph는 Canvas 기반 2D 네트워크로 렌더링하고 휠 확대·축소, 드래그 패닝, 호버 궤도선 강조와 상세 툴팁을 제공한다.
- PC Orbit Graph 화면은 `h-screen overflow-hidden`으로 페이지 스크롤을 차단하고 관련 메모 목록만 `max-h-[300px] overflow-y-auto`를 사용한다.
- 모바일 Orbit Graph는 Canvas 조작을 사용하지 않고 스냅 방식 우주 링 카러셀과 연관 메모 바텀시트를 제공한다.
- 모바일 연관 메모 바텀시트는 목록 스크롤과 분리된 고정 헤더, Body Scroll Lock, 딤드 클릭 닫기를 지원한다.
- 바텀시트 표면은 `rgba(26, 29, 38, 0.85)`, `backdrop-blur-md`, `Orbit Line` 테두리와 Deep Space 딤드를 사용한다.
- PC Timeline Stream은 미완성 궤도와 다시 볼 주요 메모를 2열 와이드 그리드로 배치한다.
- 모바일 Timeline Stream은 두 실행 리포트 카드를 `w-full` 단일 열로 쌓고 가로 넘침을 차단한다.

#### 스크롤바

- PC 스크롤바는 너비와 높이 6px, 투명 트랙, `#2A2E3D` 원형 썸을 사용하고 호버 시 `#E5A93C`로 변경한다.
- 모바일·태블릿은 가로 스크롤 동작을 유지하되 `scrollbar-width: none`과 WebKit 스크롤바 숨김을 적용한다.

### 2.3 커스텀 DatePicker 및 접근성

- 브라우저 기본 날짜 선택 UI에 의존하지 않고 `Nebula Slate` 글래스모피즘 스타일의 커스텀 달력 팝오버를 사용한다.
- 날짜 필드는 커스텀 팝오버와 별개로 키보드 직접 입력을 허용하며 `YYYY-MM-DD` 또는 구분자 없는 숫자 8자리 형식을 지원한다.
- 숫자 8자리 입력은 유효한 `YYYY-MM-DD` 값으로 정규화하고 존재하지 않는 날짜는 오류 메시지와 `aria-invalid`로 안내한다.
- 날짜 입력, 달력 열기, 이전·다음 달 이동, 날짜 선택, 팝오버 닫기는 키보드만으로 수행할 수 있어야 한다.
- 달력 팝오버는 적절한 대화형 역할과 접근 가능한 이름을 제공하고 현재 날짜, 선택 날짜, 비활성 날짜를 색 이외의 상태로도 전달한다.
- `Escape` 키로 팝오버를 닫고 닫은 뒤에는 팝오버를 열었던 날짜 필드 또는 버튼으로 포커스를 복원한다.
- 날짜 필드의 지우기(`X`) 버튼은 명확한 접근 가능한 이름을 가지며 선택을 해제해 전체 기간 조회 상태로 되돌린다.
- 열린 PC DatePicker 필드와 달력 팝오버는 `z-50` 이상의 독립 클릭 레이어와 `pointer-events-auto`를 사용하여 리포트 카드에 가려지지 않아야 한다.
- PC 달력은 날짜 필드 바로 아래 `top-full`, `left-0`, 8px 간격으로 열리며 외부 영역을 누르면 클릭 대상의 동작을 막지 않고 달력만 닫힌다.
- 날짜 선택 카드 자체는 `relative z-10`을 사용하고, 달력은 부모 스태킹 컨텍스트에 갇히지 않도록 `document.body` 포털의 전역 `z-50` 레이어에 렌더링한다.
- 스크롤과 화면 크기 변경 시 날짜 필드의 좌표를 다시 측정해 PC 달력을 필드 바로 아래에 유지한다.
- 시작일 또는 종료일만 선택한 경우에도 유효한 단일 경계 검색을 지원하고, 시작일이 종료일보다 늦으면 즉시 오류를 안내한다.

### 2.4 새 메모 진입점

- 모바일·태블릿에서는 상단과 드로어 내부에 새 메모 버튼을 두지 않고 우측 하단 고정 FAB만 사용한다.
- 모바일 FAB는 `Orbit Gold` 배경, `+` 아이콘, 깊은 드롭 섀도우, 최소 44×44px 이상의 터치 영역을 사용한다.
- PC에서는 MemoOrbit 브랜드 바로 아래 LNB에 `+ 새 메모 작성` 메인 버튼을 고정한다.
- PC 새 메모 버튼은 어두운 골드 호버 상태와 `Ctrl/⌘ + N` 키보드 단축키 툴팁을 제공한다.
- PC 전체 화면 에디터의 실제 편집 폼은 중앙 `max-w-2xl`로 제한하고 모바일·태블릿에서는 화면 폭 100%를 유지한다.
- PC 편집 폼은 `h-[75vh]`, `max-h-[80vh]` 범위의 슬림한 카드로 표시하고 편집 본문 영역만 세로 스크롤한다.

### 2.5 모바일 하단 탐색과 브랜드 홈

- 모바일·태블릿은 화면 하단에 메모 목록·생각 궤도·시간 궤도 3개 탭을 고정하고 현재 탭을 Orbit Gold로 구분한다.
- 하단 탭 바는 `z-40`, Deep Space 반투명 배경, 글래스 블러, Orbit Line 상단선을 사용하고 기기 하단 안전 영역을 포함한다.
- 모바일 상단과 PC LNB의 `MemoOrbit` 브랜드를 누르면 언제나 메모 목록 첫 화면으로 이동한다.
- 모바일 새 메모 FAB는 하단 탭 바 위에 배치하여 탭을 가리지 않는다.

### 2.6 스티키 목록 도구와 Memory Orbit 노출

- 메모 목록 화면에서는 전역 헤더의 중복 `메모 목록` 문구를 표시하지 않는다.
- 모든 메모·생각 궤적·시간 궤도 본문 헤더는 공통 `MainContentHeader`를 사용한다.
- 공통 본문 헤더는 황금색 대문자 영문 라벨, 24px 한글 제목과 선택 뱃지, 14px 보조 설명의 동일한 3단 구조로 구성한다.
- 모든 메모 헤더에는 `ALL MEMOS`, `모든 메모`, 전체 개수 뱃지, 기록 관리 설명을 표시하고 보기 토글을 제목 행 우측에 배치한다.
- 생각 궤적 헤더에는 `ORBIT GRAPH`와 태그 행성을 따라 연결된 생각을 탐색하는 설명을 표시한다.
- 시간 궤도 헤더에는 `TIMELINE STREAM`과 지정 기간의 성찰·실행 궤도를 분석하는 설명을 표시한다.
- PC 목록 도구는 `z-20`에서 스크롤이 1px 이상 발생하면 `top-0`, Orbit Line 테두리, 글래스 배경과 그림자를 활성화하고 최상단으로 돌아오면 부드럽게 해제한다.
- 모바일의 메모 목록·생각 궤적·시간 궤도 전역 헤더는 모두 `top-0`, 높이 `48px`, `z-30`, Deep Space 배경과 Orbit Line 하단선을 사용한다.
- 메모 목록 보기 도구는 모바일 전역 헤더 바로 아래인 `top-12`에 고정하여 두 헤더가 겹치지 않게 한다.
- `오늘의 Memory Orbit`은 사진 카드 보기에서만 최상단 단일 감성 카드로 노출하고 텍스트 리스트에서는 렌더링하지 않는다.

## 3. 에디터 요구사항

### 3.1 단일 리치 텍스트 캔버스

- 제목과 본문을 하나의 `contentEditable` 캔버스에서 편집하고 첫 줄은 제목으로 저장한다.
- 굵게, 기울임, 밑줄, 취소선, 글자·강조색, 문단, 목록, 체크리스트, 표를 지원한다.
- 문자 서식은 `window.getSelection()`으로 확인한 캔버스 내부의 비어 있지 않은 선택 범위에만 적용한다.
- 서식 버튼을 누르는 동안 선택 범위를 보존한다.
- 캔버스에는 브라우저의 기본 터치·마우스 드래그 선택을 방해하는 스와이프나 제스처를 등록하지 않는다.
- 모바일 편집기 헤더는 불필요한 상단 여백 없이 `pt-3 pb-2 px-4`를 사용해 상단 액션을 슬림하게 배치한다.
- 뒤로가기나 에디터 닫기를 실행할 때 한 글자 이상의 내용이 있으면 현재 제목·본문·서식·태그·이미지 설정을 즉시 저장하고 `updatedAt`을 갱신한다.

### 3.2 모바일 포맷 도구

- 하단 빠른 실행 영역에는 실제 편집 동작인 `가가`, `체크리스트`, `표`, `사진·파일 첨부`만 배치하며 독립 AI 버튼을 두지 않는다.
- `가가` 패널에는 본문을 가리는 딤이나 전체 화면 오버레이를 사용하지 않는다.
- 패널이 열린 상태에서도 본문과 실시간 서식 결과가 보인다.
- `가가` 패널은 문단 스타일, 문자 서식과 색상, 목록·들여쓰기·표의 3행 구조와 우측 상단 닫기 버튼을 사용한다.

### 3.3 체크리스트·표·태그

- 체크리스트는 원형 체크 아이콘과 독립된 텍스트 편집 영역으로 구성한다.
- 원형 아이콘을 직접 누를 때만 완료 상태가 바뀌며 행의 나머지 영역은 커서 이동과 입력에 사용한다.
- 체크리스트를 삽입하면 커서는 원형 체크 아이콘 바로 오른쪽 텍스트 시작점에 놓인다.
- 완료 항목은 원형 아이콘의 체크 상태로만 구분하고 텍스트 취소선은 사용하지 않는다.
- 표는 빈 2×2 셀, 폭 100%, 고정 레이아웃, 셀 안 자동 줄바꿈을 사용한다.
- 표 셀 클릭 또는 우클릭 시 행·열 추가/삭제, 셀 강조 포맷, 복사·오려두기·붙여넣기 메뉴를 제공한다.
- 추천 태그는 누를 때 적용과 해제를 토글한다.
- 적용 태그는 진한 브랜드 배경과 흰 글자, 미적용 태그는 밝은 배경과 선명한 아웃라인으로 구분한다.
- AI 추천 태그는 에디터 본문 하단에 상시 노출하며 추천 칩·직접 입력·이미지 무드 옵션을 한 영역에 제공한다.
- 추천 태그 칩은 한 번 클릭하면 현재 메모의 태그에 추가되거나 제거된다.
- 추천 태그는 선택하지 않아도 메모 저장 가능 여부와 내용 저장에 영향을 주지 않는다.
- 본문 분석은 입력이 멈춘 뒤 200ms 디바운스로 실행하고 본문에서 동적으로 계산한 반복 핵심어를 추천한다.
- 추천 태그는 고정 주제별 정규식 표를 사용하지 않고 본문에 실제 등장한 낱말의 빈도, 여러 문장에 걸친 반복, 첫 등장 위치와 표기 길이를 점수화해 3~5개를 동적으로 추출한다.
- 운영 분석은 `app/api/gemini` 서버 Route에서 `GEMINI_API_KEY`를 사용하며 API 키를 클라이언트 번들에 포함하지 않는다.
- Route는 요청 모델인 `gemini-2.5-flash`를 우선 사용하고, Google이 신규 키에 모델 종료 404를 반환하는 환경에서는 공식 현행 `gemini-3.6-flash`로 자동 전환한다.
- 에디터는 입력이 멈춘 뒤 300ms에 Gemini 분석을 요청하고 새 입력이 시작되면 이전 요청을 취소한다.
- Gemini는 JSON Schema에 맞춰 핵심 태그 3~5개와 감정·주제 한 줄 코멘트를 반환하며 키 누락, 호출 실패 또는 응답 검증 실패 때만 로컬 동적 핵심어 분석기로 대체한다.
- 분석 중에는 상태 문구와 틴트 펄스를, 새 추천 칩에는 짧은 페이드 효과를 제공하며 동작 줄이기 환경을 존중한다.
- 숨겨지는 서식·옵션 레이어는 `inert`로 포커스를 차단하고 닫기 전에 활성 요소의 포커스를 해제한다.

### 3.4 사진 첨부와 AI 이미지

- `가가`, `체크리스트`, `표` 빠른 실행 줄에 `사진` 버튼을 함께 배치한다.
- 사용자가 선택한 이미지 파일은 에디터 상단에서 즉시 미리 보고 제거할 수 있다.
- 이미지가 없는 텍스트 메모는 본문 문맥을 이미지 생성 요청으로 변환하고 결과 URL을 `memo.aiImageUrl`에 캐싱한다.
- AI 생성 무드는 `수채화`, `네온`, `흑백`, `빈티지` 중에서 선택하며 `memo.aiImageMood`에 저장한다.
- `memo.aiImageSourceText`에 생성 당시 본문을 기록하여 본문 변경 여부를 판단한다. 본문이 바뀌면 기존 AI 이미지 캐시를 무효화하고 재분석 대상으로 표시한다.
- 직접 첨부한 `memo.imageUrl`은 AI 이미지보다 우선 표시한다.
- 로컬 파일의 Data URL은 현재 목업 미리보기용이며 운영 환경에서는 객체 스토리지 업로드 URL로 교체한다.
- 2단계 기반 구현에서는 본문과 무드의 안정적인 해시를 사용한 로컬 이미지 생성 어댑터를 제공하고, 운영 AI 제공자 API는 이 어댑터 경계에서 교체한다.
- 생성된 `aiImageUrl`, `aiImageSourceText`, `aiImageMood`는 메모와 함께 브라우저 영구 저장소에 보존한다.

## 4. 메모 목록과 관리

- 메모 목록 상단에서 라인 구분선 기반 텍스트 리스트와 이미지 중심 갤러리 카드 보기를 전환한다.
- 갤러리 보기는 직접 첨부 이미지 또는 `aiImageUrl`을 큰 4:3 미디어 영역에 표시하고 이미지가 없으면 브랜드 그라데이션을 사용한다.

- 목록은 카드 그리드가 아닌 연도·월·기간별 둥근 그룹 패널이며 각 메모 행은 얇은 시스템 구분선으로 나눈다.
- 오른쪽 스와이프에는 노란색 고정 동작, 왼쪽 스와이프에는 회색 더 보기와 빨간색 삭제 동작을 노출한다.

- 메모는 수정 시각 최신순으로 정렬하고 고정 메모를 일반 메모 위에 배치한다.
- 고정 메모 섹션은 `aria-expanded`를 가진 아코디언으로 접고 펼친다.
- 고정 섹션 헤더는 고정 높이와 줄 높이를 사용하여 아코디언 전환 시 타이틀 위치가 움직이지 않는다.
- 카드 본문에 중복되는 `고정됨` 텍스트를 표시하지 않는다.
- 모든 디바이스에서 핀 버튼으로 고정 여부를 바꿀 수 있고 터치 환경에서는 스와이프도 지원한다.
- 모바일·태블릿 목록은 둥근 카드나 외곽선 없이 행 사이 구분선으로 나눈다.
- 데스크톱 목록은 카드 표면과 수정·공유·삭제 작업 버튼을 사용한다.
- 데스크톱 메모 행은 우측 상단 호버 핀과 우클릭 메뉴에서 고정·고정 해제를 제공한다.
- 삭제는 브라우저 기본 확인창 대신 커스텀 모달을 사용한다.
- 제목만 있는 메모에는 `추가 텍스트 없음`을 표시한다.
- 목록 날짜는 `2026.09.01` 형태의 `YYYY.MM.DD` 형식으로 통일한다.
- 데스크톱 LNB의 `새 메모 작성` 버튼은 브랜드 색을 유지하고 호버 시 자연스러운 어두운 골드로 바뀐다.

### 4.1 날짜 그룹 규칙

- 정렬과 그룹 판정은 모두 `updatedAt`을 사용한다. 오래된 메모도 수정하면 현재 시각으로 갱신되어 `오늘` 최상단으로 이동한다.
- 현재 연도 메모는 `오늘`, `어제`, `이전 7일`, `이전 30일`, 이후 `1월`부터 `12월`까지 월별 헤더를 사용한다.
- 작년 이전 메모는 월을 나누지 않고 `2025년`, `2024년`처럼 연도별 헤더로 묶는다.
- 그룹 안에서는 최신 수정 시각순을 유지한다.

### 4.2 추억 메모 경험

- `오늘의 이 날`은 현재 날짜를 기준으로 정확히 1년 전, 100일 전 등 의미 있는 간격의 메모를 상단 추천 카드로 보여 준다.
- 같은 날짜 후보가 여러 개면 이미지 존재 여부, 고정 여부, 내용 충실도 순으로 대표 추억을 선택한다.
- 사용자가 추천을 숨기거나 다시 보고 싶지 않음을 선택할 수 있게 하여 반복 노출 피로를 줄인다.
- 추억 뷰어는 별도 보기 선택 탭 없이 하나의 감성 폴라로이드 슬라이드 카드로 제공한다.
- 추억 후보는 수정 시각이 아닌 `createdAt`을 기준으로 정확히 1년 전과 100일 전을 계산한다.
- 내보내기는 이미지, 제목, 날짜, 선택 태그를 하나의 공유용 이미지로 합성하며 원본 메모 내용과 개인정보 포함 여부를 미리 확인하게 한다.

### 4.3 시간 궤도 실행 리포트

- 기간 선택은 전체 기간 해제와 단일 경계 검색을 유지하며 잘못된 시작일·종료일 조합은 즉시 안내한다.
- `AI 감정 & 생각 궤도 분석`은 사용자 태그와 동적 본문 핵심어의 분포, 질문과 미완료 항목 비율을 종합해 주제 비중과 한 줄 총평을 제공한다.
- 시간 궤도는 선택 기간의 날짜·제목·본문·태그를 하나의 문맥으로 합쳐 Gemini에 전달하고 모델이 생성한 기간 총평을 우선 표시한다.
- `AI 잊혀진 아이디어 큐레이션`은 질문형 문장 또는 완료되지 않은 체크리스트가 있는 과거 메모를 점수화해 대표 2개를 다시 제안한다.
- 큐레이션 항목은 선정 이유, 원본 메모 제목과 핵심 문장을 보여 주며 클릭하면 해당 메모 편집기를 즉시 연다.
- 단순 KPI 수치 나열, 장식 목적의 월별 차트, 외부 AI 프롬프트 복사 영역은 제공하지 않는다.

## 5. 구현 검수 체크리스트

- [x] iOS 메모형 상단 편집기 도구와 중앙 날짜 적용
- [x] 검은색 단일 편집 캔버스와 키보드 인접 하단 도구 적용
- [x] 3행 포맷 Bottom Sheet와 슬라이드 닫기 적용
- [x] 그룹 패널 목록과 양방향 스와이프 동작 적용
- [x] MemoOrbit 브랜드 복구 및 외부 서비스명 제거
- [x] PC 중앙 고정 폭과 호버·우클릭 핀 동작 적용
- [x] 체크리스트 입력 커서 정렬 및 완료 취소선 제거
- [x] 표 행·열·클립보드·포맷 컨텍스트 메뉴 적용
- [x] AI 추천 키워드와 직접 태그 입력 영역 복구
- [x] 독립 AI 툴바 버튼 제거 및 추천 태그 상시 고정 영역 전환
- [x] 숨김 레이어 `inert` 및 닫기 전 포커스 해제
- [x] 본문·무드 기반 이미지 생성 어댑터와 원문 비교 캐시 적용
- [x] 메모와 AI 이미지 캐시 브라우저 영구 저장
- [x] 1년 전·100일 전 추억 후보 추출 로직 적용
- [x] 단일 감성 슬라이드 Memory Orbit 뷰어 적용
- [x] 모바일 드로어와 PC 고정 LNB 메뉴 체계 적용
- [x] 메모 리스트·이미지 갤러리 2타입 보기 적용
- [x] 태그 행성 노드 Orbit Graph 탐색 적용
- [x] 날짜 범위 기반 미완성 궤도·다시 볼 메모 Timeline Stream 적용
- [x] Deep Space 글래스모피즘 커스텀 DatePicker 및 키보드 직접 입력 적용
- [x] DatePicker 지우기·전체 기간 조회와 팝오버 포커스 복원 적용
- [x] PC Canvas Orbit Graph 줌·패닝·호버 상세 탐색 적용
- [x] 모바일 우주 링 카러셀과 연관 메모 바텀시트 적용
- [x] 모바일 바텀시트 고정 헤더·배경 스크롤 차단·딤드 닫기 적용
- [x] PC LNB 새 메모 버튼과 모바일 FAB 진입점 분리
- [x] 시간 궤도 실행 리포트 PC 2열·모바일 1열 레이아웃 적용
- [x] 메모 목록 스티키 보기 도구와 갤러리 전용 Memory Orbit 노출 적용
- [x] 모바일 중첩 스티키 오프셋과 에디터 상단 안전 영역·가로 오버플로 차단 적용
- [x] Orbit Graph PC 연관 메모 세로 목록과 모바일 총개수 뱃지 적용
- [x] 200ms 실시간 추천 태그와 핵심어·기술·감정 추출 적용
- [x] 미완료 체크 항목과 핵심 아이디어 자동 추출 적용
- [x] 기록 밀도·태그 집중도 기반 대표 메모 3개 큐레이션 적용
- [x] PC 1px 스크롤 감지 스티키 목록 도구와 테두리 전환 적용
- [x] PC 에디터 중앙 `max-w-2xl` 폭 제한 적용
- [x] DatePicker 상위 클릭 레이어와 포인터 이벤트 정상화
- [x] 시간 궤도 리포트 항목에서 원본 메모 편집기 바로 열기 적용
- [x] 전역 z-index 0–60 레이어 계층 표준화
- [x] PC Orbit Graph 페이지 스크롤 차단과 300px 내부 목록 스크롤 적용
- [x] PC 6px 다크 스크롤바와 모바일 스크롤바 숨김 적용
- [x] 메모 행의 빨간 배경 누출 제거 및 Orbit Line 통일
- [x] 목록 날짜 `YYYY.MM.DD` 표준화
- [x] PC 에디터 `75vh`·최대 `80vh` 슬림 카드와 본문 내부 스크롤 적용
- [x] 모바일 3개 주요 화면 공통 48px 스티키 헤더 적용
- [x] 모바일 에디터 헤더 `pt-3 pb-2 px-4` 여백 적용
- [x] 내용이 있는 에디터 이탈 시 자동 저장과 수정 시각 갱신 적용
- [x] 모바일 3대 메뉴 하단 탭 바와 브랜드 홈 이동 적용
- [x] DatePicker 필드 하단 정렬·외부 클릭 닫기·최상위 클릭 레이어 적용
- [x] 세 메인 화면 공통 3단 콘텐츠 헤더와 메모 보기 액션 슬롯 적용
- [x] 고정 태그 정규식 삭제와 동적 본문 핵심어 3~5개 추출 적용
- [x] AI 생각 스펙트럼 한 줄 총평과 잊혀진 아이디어 2개 큐레이션 적용
- [x] 핵심 State·이벤트·배열 가공 로직의 초보자용 한글 주석 적용
- [x] 날짜 카드 `z-10`과 body 포털 달력 `z-50`의 독립 레이어 적용
- [x] 공통 네비게이션 `z-30`·페이지 타이틀 `z-20`·모바일 탐색 `z-40` 계층 적용
- [x] 모바일 카드 불투명 전면 배경과 카드 너비 40% 자동 고정 스와이프 적용
- [x] 공통 영문 콘텐츠 라벨 상단 24px 여백과 날짜 필드 SVG 달력 아이콘 적용
- [x] 편집기·DatePicker·연관 메모 바텀시트의 바디 스크롤 잠금과 딤드 닫기 보정
- [x] 서버 전용 Gemini 2.5 Flash JSON 분석 Route와 환경 변수 안전 장치 적용
- [x] 에디터 300ms Gemini 태그 분석·요청 취소·로컬 실패 대체 적용
- [x] 선택 기간 메모 Gemini 총평과 로컬 실패 대체 상태 안내 적용

- [x] 선택 범위에만 문자 서식 적용
- [x] 편집 캔버스의 드래그 선택 방해 제스처 제거
- [x] 모바일 `가가`·`체크리스트`·`표` 빠른 버튼 배치
- [x] 모바일 포맷 패널 딤·오버레이 제거
- [x] 체크박스 아이콘과 텍스트 영역 클릭 분리
- [x] 추천 태그 활성·비활성 색상 및 접근성 상태 구분
- [x] 모바일·태블릿 전체 화면 편집기와 구분선 목록 적용
- [x] 데스크톱 새 메모 호버 색상 정상화
- [x] 태블릿을 모바일 UI 범위에 통합
- [x] 4.5:1 이상 텍스트 대비 적용
- [x] 고정 메모 아코디언과 중복 문구 제거
- [x] 전 디바이스 핀 버튼 및 터치 스와이프 고정 제공
- [x] 10개 주제별 시나리오 목업 데이터 100개 구성
- [x] 오늘·어제·최근 구간·월별·과거 연도 그룹 규칙 적용
- [x] 사진 첨부 버튼, 로컬 미리보기와 제거 기능 적용
- [x] 이미지 무드 선택 및 이미지 캐시 확장용 타입 정의
- [x] `npx tsc --noEmit` 통과
- [x] `npx eslint .` 통과

## 6. 3단계 구현 로드맵

### 1단계: 기록과 탐색 기반

- [x] 단일 리치 텍스트 캔버스와 선택 영역 서식
- [x] 체크리스트·표·추천 태그와 반응형 목록
- [x] 고정 메모 아코디언과 최신 수정순 재배치
- [x] 100개 주제별 목업 데이터와 지능형 날짜 그룹

### 2단계: 이미지가 있는 추억 메모

- [x] 사진 선택, 에디터 미리보기, 메모 이미지 필드 기반
- [x] 수채화·네온·흑백·빈티지 무드 선택 상태
- [ ] 객체 스토리지 업로드 및 이미지 최적화
- [x] 본문 문맥·무드 기반 이미지 생성 어댑터와 `aiImageUrl` 브라우저 영구 캐싱
- [x] `aiImageSourceText` 비교 기반 본문 변경 시 캐시 무효화·재생성
- [ ] 운영 AI 이미지 제공자 API 및 작업 큐 연동, 실패 재시도

### 3단계: 회상과 공유 경험

- [x] 1년 전·100일 전 후보를 계산하는 `오늘의 이 날` 추천 엔진 기초
- [ ] 상단 추억 카드의 숨기기·다시 보지 않기 설정
- [x] 단일 감성 폴라로이드 슬라이드 뷰어 기초
- [ ] 개인정보 확인 단계를 포함한 공유 이미지 내보내기
- [ ] 의미 기반 연관 메모 검색(Orbit Search)
- [ ] 사용자 인증 및 서버 영속 저장

## 7. 보완 아이디어

- AI 이미지 생성은 저장 직후 동기 대기시키지 않고 작업 큐에서 처리하여 메모 저장 속도를 유지한다.
- 같은 본문과 무드 조합에는 콘텐츠 해시를 사용해 중복 이미지 생성 비용을 줄인다.
- 추억 추천은 민감 태그와 사용자가 숨긴 날짜를 제외하는 안전 필터를 둔다.
- 이미지 내보내기는 원본과 별도의 공유용 해상도를 만들고 위치 정보 같은 파일 메타데이터를 제거한다.
- 100개 이상 목록에서도 그룹 계산이 반복되지 않도록 정렬·그룹 결과를 메모이제이션한다.
