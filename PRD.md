# 🚀 MemoOrbit (메모오빗) 제품 기획서 (PRD)

## 1. 서비스 개요 & 비전
- **서비스명**: MemoOrbit (생각의 궤적을 기록하고 연결하는 AI 메모 서비스)
- **핵심 가치**: 아이폰 메모 앱의 서식 에디터 UX에 AI 기반 문맥 분석 및 '연관 생각 궤적 검색(Semantic Orbit Search)'을 결합하여, 단순 기록을 넘어 뇌의 의식 흐름대로 생각을 확장하는 반응형 메모 플랫폼.
- **디바이스 범위**: 모바일 퍼스트 반응형 웹 (PC/태블릿/모바일 전 디바이스 대응).

---

## 2. 주요 기능 명세 (Feature Specifications)

### A. 아이폰 메모 앱 라이크 에디터 & 서식 툴바 (Apple Notes Rich Editor)
- **단일 통합 입력 캔버스**: Title/Content 구분 없이 단일 입력창 구성. 첫 줄은 대표 제목(Bold), 둘째 줄부터 본문 처리.
- **리치 서식 툴바 (Formatting Toolbar)**:
  - 텍스트 서식: 제목/머리말/본문 크기 전환, 굵게(Bold).
  - 체크리스트: 클릭하여 완료/미완료 토글 가능한 체크박스 항목 생성.
  - 표(Table): 행과 열 기반의 미니 격자 표 삽입 및 내용 편집.
- **서브 텍스트 예외 처리**: 첫 줄만 작성 시 카드 리스트 둘째 줄에 "추가 텍스트 없음" 안내 표기.
- **빈 메모 파기**: 내용 없이 창을 닫을 경우 미입력 빈 메모 자동 삭제.

### B. 메모 관리 & 정렬 (Sorting & Edge Cases)
- **상단 메모 고정 (Pinning)**: 중요 메모를 최상단 '고정된 메모' 섹션에 배치.
- **최신순 덮어쓰기 정렬**: 작성/수정 시 해당 시각으로 업데이트되며 별도 `(수정됨)` 표기 없이 최상단 자동 재배치.
- **날짜별 그룹화**: '오늘', '이전 7일', '이전 30일', 'MM월' 그룹 헤더 제공.
- **전체 카운터**: 헤더/사이드바에 `전체 N개의 메모` 표기.
- **커스텀 삭제 모달**: 브라우저 기본 `confirm` 대신 디자인 통일 커스텀 삭제 팝업 적용.
- **PC 커서 UX**: 모든 버튼 및 클릭 요소에 `cursor: pointer` 전역/컴포넌트 스타일 적용.

### C. 차별화된 AI 스마트 시스템 (AI & Semantic Search)
- **문맥 감지 동적 AI 태그**: 텍스트 전체를 분석하여 주제/감정에 맞는 동적 태그(#운동후기, #다이어트고민 등) 실시간 추천 및 자석 토글.
- **의식의 흐름 연관 검색 (Orbit Semantic Search)**:
  - 단순 글자 일치 검색을 뛰어넘는 지능형 검색.
  - 검색어 입력 시 해당 키워드뿐만 아니라 의미론적으로 연결된 생각, 연관 태그, 관련 맥락의 메모들을 궤적(Orbit)으로 연결하여 함께 제안/표시.

### D. 모바일 안정성, 접근성 및 확장 구조
- **모바일 모달 안전 영역**: 모달이 열리면 배경 스크롤을 차단하고, `max-h-[90vh]` 내부 스크롤과 Safe Area를 적용한다.
- **전체 폭 뷰포트**: 모바일에서 `w-full min-w-full`을 유지하여 좌우 잘림과 의도하지 않은 가로 스크롤을 방지한다.
- **실시간 추천 태그**: 본문 입력을 디바운스하여 문맥 키워드 기반 추천 태그를 즉시 갱신한다.
- **실시간 리치 렌더링**: 마크다운 기호를 노출하지 않고 제목, 머리말, 본문, 굵게, 체크리스트, 표를 편집 화면과 카드에 실제 서식으로 렌더링한다.
- **디바이스별 상호작용**: 포인터 기기에는 hover와 pointer를, 터치 기기에는 hover 제거와 active 피드백을 제공한다.
- **웹 접근성**: 모든 대화상자에 dialog 역할과 modal 상태를 지정하고, 명확한 ARIA 이름 및 4.5:1 이상의 텍스트 대비를 제공한다.
- **인증 확장성**: `Memo` 모델에 선택적 `userId`를 두어 추후 다중 사용자 데이터 분리를 지원한다.
- **디자인 시스템**: 쨍한 원색 대신 Neutral Gray, Stone, Sand 계열의 차분한 색상 체계를 사용한다.
- **모바일 에디터 고정 프레임**: 헤더, 서식 툴바, 태그 및 저장 영역은 고정하고 본문 캔버스만 독립적으로 스크롤한다.
- **모바일 전체 화면 편집기**: 모바일에서는 배경 딤과 팝업 외곽 여백을 제거하고 편집기를 `100dvh` 전체 화면으로 표시하며, PC에서는 중앙 모달 형태를 유지한다.
- **표 레이아웃 안정성**: 표는 더미 문구 없는 빈 셀로 생성하며, 컨테이너 100% 고정 레이아웃과 셀 내부 자동 줄바꿈을 적용한다.
- **Apple Notes 체크리스트**: 기본 체크박스 대신 원형 체크 UI, 토글 애니메이션 및 완료 문구 취소선을 제공한다.
- **모바일 카드 스와이프**: 카드 터치 시 편집기로 진입하고, 오른쪽 스와이프는 고정 토글, 왼쪽 스와이프는 삭제 및 공유 액션을 노출한다.

---

## 3. 단계별 구현 로드맵 (Roadmap)

### [1단계: 단일 에디터 & 핵심 UX 개편]
- [x] 제목/내용 통합 입력창 구성 (`MemoModal.tsx`).
- [x] 메모 고정(Pin) 및 '추가 텍스트 없음' 예외 처리.
- [x] 시간 덮어쓰기 최신순 정렬 및 날짜별 그룹 헤더 UI 구축.
- [x] 커스텀 삭제 모달 적용.
- [x] 서식 툴바 기능(글자 크기/굵기, 체크리스트, 표 삽입) 구현.
- [x] 클릭 가능 요소 전반에 `cursor: pointer` 스타일 보완.
- [x] 모바일 모달 바디 스크롤 잠금, 최대 높이, Safe Area 및 전체 폭 적용.
- [x] 디바운스 기반 실시간 문맥 추천 태그 구현.
- [x] 마크다운 문자를 노출하지 않는 리치 텍스트 편집 및 카드 렌더링 구현.
- [x] PC hover/pointer와 모바일 active 피드백 분리.
- [x] 대화상자 ARIA, 명도 대비 및 한국어 문서 메타데이터 개선.
- [x] `Memo.userId` 인증 확장 필드 추가.
- [x] Neutral Gray / Stone / Sand 기반 디자인 시스템 적용.
- [x] 본문만 스크롤되는 모바일 에디터 고정 프레임 적용.
- [x] 모바일 딤 제거 및 `100dvh` 전체 화면 편집기 적용.
- [x] 빈 셀 기반 고정 폭 표와 자동 줄바꿈 구현.
- [x] 원형 체크박스, 토글 애니메이션 및 완료 취소선 구현.
- [x] 터치 기반 좌우 스와이프와 카드 터치 편집 진입 구현.

### [2단계: AI 문맥 태그 및 지능형 연관 검색]
- [ ] AI 문맥 감지 태그 추천 및 자석 토글.
- [ ] 키워드 및 연관 맥락/태그를 묶어 궤적으로 연동해 보여주는 지능형 연관 검색(Orbit Search) 구현.

### [3단계: 데이터 영속성 & 반응형 패널 완성]
- [ ] `LocalStorage` 연동을 통한 데이터 영구 보존.
- [ ] PC/모바일 반응형 2단 레이아웃 구현.
